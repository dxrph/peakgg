export default function Home() {
  return (
    <main className="home-root">
      {/* 01 — Navigation */}
      <section data-section="navigation" className="home-slot" />
      {/* 02 — Hero */}
      <section data-section="hero" className="home-slot" />
      {/* 03 — Live strip */}
      <section data-section="live-strip" className="home-slot" />
      {/* 04 — Open Cup */}
      <section data-section="open-cup" className="home-slot" />
      {/* 05 — Live match */}
      <section data-section="live-match" className="home-slot" />
      {/* 06 — Rank is identity */}
      <section data-section="rank-identity" className="home-slot" />
      {/* 07 — Find your five */}
      <section data-section="find-your-five" className="home-slot" />
      {/* 08 — Moments define us */}
      <section data-section="moments" className="home-slot" />
      {/* 09 — Upcoming tournaments */}
      <section data-section="upcoming-tournaments" className="home-slot" />
      {/* 10 — Enter Peak */}
      <section data-section="enter-peak" className="home-slot" />
      {/* 11 — Final bar */}
      <section data-section="final-bar" className="home-slot" />
    </main>
  );
}
