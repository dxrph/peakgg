import { useTranslation } from "react-i18next";
import art from "../assets/peak-arena-editorial.jpg";
import { upcomingTournaments } from "../data/mock";
interface Props { onRegister: () => void }
export default function UpcomingSection({onRegister}:Props){const{t,i18n}=useTranslation();const format=(date:string)=>new Intl.DateTimeFormat(i18n.language.startsWith("it")?"it-IT":"en-GB",{day:"2-digit",month:"short",timeZone:"UTC"}).format(new Date(`${date}T00:00:00Z`)).replace(".","").toUpperCase();return <section className="editorial-section upcoming-section"><header><span className="section-kicker">07 / {t("upcoming.kicker")}</span><h2>{t("upcoming.title")}</h2><button type="button" onClick={onRegister}>{t("upcoming.viewAll")} ↗</button></header><div className="upcoming-list">{upcomingTournaments.map((event,index)=><button type="button" onClick={onRegister} key={event.id}><span>0{index+1}</span><time>{format(event.date)}</time><strong>{t(event.nameKey)}</strong><em>{t(event.statusKey)}</em><b>→</b></button>)}</div><div className="upcoming-art"><img src={art} alt="" loading="lazy" width={1600} height={1000}/></div></section>}