import Image from 'next/image';import Link from 'next/link';import {tournamentHref,type Tournament} from '@/data/tournaments';import TournamentTimeline from './TournamentTimeline';
export default function FeaturedTournament({tournament}:{tournament:Tournament}){const count=tournament.registered??0,capacity=Number(tournament.capacity.split(' ')[0]),href=tournamentHref(tournament);return <section className="t-featured" id="featured-event" aria-labelledby="featured-title"><div className="t-wrap"><div className="t-featured-grid"><div className="t-featured-art"><Image src={tournament.image} alt={tournament.imageAlt} fill priority sizes="(max-width:600px) 100vw, 58vw" style={{objectPosition:tournament.imagePosition}}/><span>{tournament.code} / FEATURED EVENT</span><b aria-hidden="true">01</b></div><div className="t-featured-info"><p className="t-registration">REGISTRATION / <strong>{tournament.status}</strong></p><h2 id="featured-title">{tournament.name}<br/>{tournament.edition}</h2><dl>{[['REGION',tournament.region],['CAPACITY',tournament.capacity],['FORMAT',tournament.format],['STRUCTURE',tournament.structure]].map(([label,value])=><div key={label}><dt>{label}</dt><dd>{value}</dd></div>)}</dl><div className="t-date"><strong>{tournament.date}</strong><span>{tournament.time||'TIME PENDING'}</span></div><div className="t-capacity"><span>{count} / {capacity} REGISTERED</span><i aria-hidden="true"><b style={{width:`${capacity?Math.min(100,count/capacity*100):0}%`}}/></i></div><div className="t-featured-actions"><Link href={`${href}?register=1`}>REGISTER TEAM</Link><Link href={href}>VIEW EVENT <span aria-hidden="true">→</span></Link></div></div></div><TournamentTimeline/></div></section>}
