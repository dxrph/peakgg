'use client';
export default function TournamentsFinalCta(){const focusOpen=()=>{const button=document.querySelector<HTMLButtonElement>('.t-tabs button:nth-child(2)');button?.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'center'});button?.focus();button?.click()};return <section className="final-cta" aria-labelledby="final-title"><div className="t-wrap final-cta-grid"><div><p>05 / NEXT MATCH</p><h2 id="final-title">BUILT FOR<br/>THE NEXT<br/><span>MATCH.</span></h2></div><div className="final-cta-action"><p>THE CIRCUIT IS OPEN.</p><button type="button" onClick={focusOpen}>VIEW OPEN EVENTS <span aria-hidden="true">↗</span></button></div></div></section>}
