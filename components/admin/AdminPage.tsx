import {EmptyState} from '../ui/ProductPage';export function AdminPage({code,title,columns}:{code:string;title:string;columns?:string[]}){return <><p className="mono-label">{code}</p><h1>{title}</h1>{columns?<table className="admin-table"><thead><tr>{columns.map(x=><th key={x}>{x}</th>)}</tr></thead><tbody><tr><td colSpan={columns.length}><EmptyState title="NO RECORDS REQUIRE ATTENTION."/></td></tr></tbody></table>:<EmptyState title="NO RECORDS REQUIRE ATTENTION."/>}</>}
