import Link from 'next/link';import {StatusBadge} from '../ui/ProductPage';
export function PlayerCard({player,canInvite=false}:{player:{username:string;region:string;primary_role:string;looking_for_team?:boolean;team?:string|null};canInvite?:boolean}){return <article className="product-card"><StatusBadge>{player.looking_for_team?'LOOKING FOR TEAM':'PLAYER'}</StatusBadge><h2>{player.username}</h2><p>{player.primary_role} / {player.region}<br/>{player.team||'FREE AGENT'}</p><Link className="peak-button secondary" href={`/players/${player.username}`}>VIEW PROFILE</Link>{canInvite&&<button className="peak-button" type="button">INVITE</button>}</article>}
export function TeamCard({team}:{team:{slug:string;name:string;tag:string;region:string;recruiting?:boolean;members?:number}}){return <article className="product-card"><StatusBadge>{team.recruiting?'RECRUITING':'TEAM'}</StatusBadge><h2>{team.name}</h2><p>{team.tag} / {team.region}<br/>{team.members??0}/7 ACTIVE ROSTER</p><Link className="peak-button secondary" href={`/teams/${team.slug}`}>VIEW TEAM</Link></article>}
