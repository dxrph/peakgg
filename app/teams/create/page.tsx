import {ProductPage} from '../../../components/ui/ProductPage';import {createTeam} from './actions';
export default async function CreateTeam({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <ProductPage eyebrow="TEAMS / CREATE" title="BUILD YOUR FIVE." description="THE CREATOR BECOMES TEAM CAPTAIN AND STARTER."><form action={createTeam} className="peak-container data-section">{q.error&&<p className="form-error" role="alert">{q.error}</p>}<Field name="name" label="TEAM NAME"/><Field name="tag" label="TEAM TAG" maxLength={6}/><div className="form-field"><label htmlFor="region">REGION</label><select id="region" name="region"><option>EU WEST</option><option>EU NORTH/EAST</option><option>EU OTHER</option></select></div><div className="form-field"><label htmlFor="description">DESCRIPTION</label><textarea id="description" name="description"/></div><div className="form-field"><label htmlFor="logo">TEAM LOGO</label><input id="logo" name="logo" type="file" accept="image/png,image/jpeg,image/webp"/></div><label><input name="recruiting" type="checkbox"/> RECRUITING ON</label><button className="peak-button">CREATE TEAM</button></form></ProductPage>}function Field(p:{name:string;label:string;maxLength?:number}){return <div className="form-field"><label htmlFor={p.name}>{p.label}</label><input id={p.name} name={p.name} maxLength={p.maxLength} required/></div>}
