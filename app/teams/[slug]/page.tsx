import {notFound} from 'next/navigation';import {ProductPage,EmptyState} from '../../../components/ui/ProductPage';import {supabaseConfigured} from '../../../lib/supabase/config';import {createClient} from '../../../lib/supabase/server';
export default async function Team({params}:{params:Promise<{slug:string}>}){const {slug}=await params;if(!supabaseConfigured)notFound();const supabase=await createClient();const {data}=await supabase.from('teams').select('*').eq('slug',slug).maybeSingle();if(!data)notFound();const t=data as Record<string,unknown>;return <ProductPage eyebrow={`${t.tag} / ${t.region}`} title={String(t.name)} description={String(t.description||'COMPETITIVE TEAM')}><section className="peak-container data-section"><EmptyState title="NO VERIFIED TEAM RESULTS YET." body="ROSTER, TOURNAMENT HISTORY, AND RECENT MATCHES WILL APPEAR AFTER COMPETITIVE ACTIVITY."/></section></ProductPage>}
