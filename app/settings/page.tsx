import {ProductPage} from '../../components/ui/ProductPage';import {requireUser} from '../../lib/auth/session';import {signOut} from '../../lib/auth/actions';
export default async function Settings(){const user=await requireUser('/settings');return <ProductPage eyebrow="PEAKGG / SETTINGS" title="SETTINGS" description={user.email||''}><section className="peak-container product-grid">{['ACCOUNT','COMPETITIVE','NOTIFICATIONS'].map(x=><article className="product-card" key={x}><p className="mono-label">{x}</p><h2>{x}</h2><p>{x==='ACCOUNT'?'EMAIL AND PASSWORD SECURITY.':x==='COMPETITIVE'?'REGION, ROLES, AVAILABILITY, AND LFT.':'IN-APP NOTIFICATION PREFERENCES.'}</p></article>)}</section><form action={signOut} className="peak-container"><button className="peak-button secondary">SIGN OUT</button></form></ProductPage>}
