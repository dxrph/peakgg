import Link from 'next/link';import AuthForm from '../../components/ui/AuthForm';import {signIn} from '../../lib/auth/actions';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;next?:string}>}){const q=await searchParams;return <AuthForm title="SIGN IN" error={q.error} footer={<><Link href="/forgot-password">FORGOT PASSWORD</Link><Link href="/register">CREATE ACCOUNT</Link></>}><form action={signIn}><input type="hidden" name="next" value={q.next||'/dashboard'}/><div className="form-field"><label htmlFor="email">EMAIL</label><input id="email" name="email" type="email" autoComplete="email" required/></div><div className="form-field"><label htmlFor="password">PASSWORD</label><input id="password" name="password" type="password" autoComplete="current-password" required/></div><button className="peak-button" type="submit">SIGN IN</button></form></AuthForm>}
