import Link from 'next/link';import AuthForm from '../../components/ui/AuthForm';import {register} from '../../lib/auth/actions';
export default async function Register({searchParams}:{searchParams:Promise<{error?:string;sent?:string}>}){const q=await searchParams;return <AuthForm title="CREATE ACCOUNT" error={q.error} footer={<Link href="/login">ALREADY HAVE AN ACCOUNT?</Link>}><>{q.sent?<p role="status">CHECK YOUR EMAIL TO CONFIRM YOUR ACCOUNT BEFORE SIGNING IN.</p>:<form action={register}><div className="form-field"><label htmlFor="email">EMAIL</label><input id="email" name="email" type="email" autoComplete="email" required/></div><div className="form-field"><label htmlFor="password">PASSWORD</label><input id="password" name="password" type="password" autoComplete="new-password" minLength={8} required/></div><div className="form-field"><label htmlFor="confirmation">CONFIRM PASSWORD</label><input id="confirmation" name="confirmation" type="password" autoComplete="new-password" minLength={8} required/></div><button className="peak-button" type="submit">REGISTER</button></form>}</></AuthForm>}

