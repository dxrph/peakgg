export default async function AdminDispute({params}:{params:Promise<{id:string}>}){const {id}=await params;return <><p className="mono-label">CONTROL ROOM / DISPUTE {id}</p><h1>DISPUTE REVIEW</h1><section className="product-grid">{['TOURNAMENT','MATCH','TEAMS','REPORTED RESULT','CHAT HISTORY','EVIDENCE','DISPUTE REASON'].map(x=><article className="product-card" key={x}><p className="mono-label">{x}</p><p>NO DATA AVAILABLE.</p></article>)}</section><div className="product-actions">{['CONFIRM TEAM A RESULT','CONFIRM TEAM B RESULT','SET CORRECT RESULT','VOID MATCH','REQUEST MORE EVIDENCE'].map(x=><button className="peak-button secondary" disabled key={x}>{x}</button>)}</div></>}
