import AuthForm from '../../components/ui/AuthForm';import {completeOnboarding} from './actions';
const roles=['DUELIST','CONTROLLER','INITIATOR','SENTINEL','FLEX'];export default async function Onboarding({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <AuthForm title="BUILD YOUR PROFILE" error={q.error} footer={<span>RIOT STATUS / UNVERIFIED</span>}><form action={completeOnboarding}><Field name="username" label="USERNAME"/><Field name="riot_game_name" label="RIOT GAME NAME"/><Field name="riot_tag" label="RIOT TAG"/><Select name="region" label="REGION" values={['EU WEST','EU NORTH/EAST','EU OTHER']}/><Select name="primary_role" label="PRIMARY ROLE" values={roles}/><Select name="secondary_role" label="SECONDARY ROLE OPTIONAL" values={['',...roles]}/><button className="peak-button">COMPLETE PROFILE</button></form></AuthForm>};function Field({name,label}:{name:string;label:string}){return <div className="form-field"><label htmlFor={name}>{label}</label><input id={name} name={name} required/></div>}function Select({name,label,values}:{name:string;label:string;values:string[]}){return <div className="form-field"><label htmlFor={name}>{label}</label><select id={name} name={name}>{values.map(v=><option key={v} value={v}>{v||'NONE'}</option>)}</select></div>}
