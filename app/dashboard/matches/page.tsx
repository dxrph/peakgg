import {ProductPage,EmptyState} from '../../../components/ui/ProductPage';import DashboardNav from '../../../components/product/DashboardNav';import {requireUser} from '../../../lib/auth/session';export default async function DashboardMatches(){await requireUser('/dashboard/matches');return <ProductPage eyebrow="DASHBOARD / MATCHES" title="MATCHES"><DashboardNav/><nav className="peak-container product-actions">{['UPCOMING','AWAITING RESULT','COMPLETED','DISPUTED'].map(x=><button className="peak-button secondary" key={x}>{x}</button>)}</nav><div className="peak-container"><EmptyState title="NO MATCHES SCHEDULED."/></div></ProductPage>}
